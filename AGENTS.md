# AGENTS.md

## 沟通与语言

- **所有回复必须使用中文**（包括代码评审意见、PR 评论、提交说明建议等）。
- 如需引用英文原文/报错信息可保留英文，但解释与结论仍用中文。

---

## 开发环境提示

### Node.js（使用 pnpm）

- 使用 `pnpm install --filter <project_name>` 将该包加入工作区依赖范围，确保 ESLint、TypeScript 等工具能正确识别。
- 创建新项目（Next.js + TypeScript）：
  - 推荐：`pnpm dlx create-next-app@latest <project_name> --ts`
  - 如需交互式选项，按提示选择即可（建议启用 ESLint、App Router 等，按项目约定为准）。
- 检查每个包内的 `package.json` 的 `name` 字段以确认正确的项目名（忽略根目录的 `package.json`）。
- 新增包要执行 `pnpm add`，不要直接修改 `package.json` 文件，保证包管理的一致性。

### Python（使用 uv）

- Python 相关依赖与环境统一使用 **uv** 管理（不要使用 pip/poetry/conda 作为主流程）。
- 常用命令示例：
  - 创建虚拟环境：`uv venv`
  - 安装依赖：`uv pip install -r requirements.txt`
  - 运行脚本：`uv run python <script.py>`
- 如果仓库中存在 `pyproject.toml` 锁文件，请优先遵循项目内既定的 uv 使用方式。

### 浏览器

- 使用 **Edge** 浏览器作为主要浏览器，Chrome 作为备用浏览器。

---

## 编码前思考

> 来源：Karpathy 观察 —— 模型会代你做错误假设，然后不假思索地执行。

**不要假设。不要隐藏困惑。呈现权衡，再动手。**

- **显式说明假设**：不确定时，先提出假设并请用户确认，而不是默默选择一种解释执行。
- **呈现多种解释**：当需求存在歧义时，列出 2–3 种理解方式，让用户选择，而不是自行猜测。
- **适时提出异议**：如果存在更简单的实现路径，明确说出来，不要因为"用户这么说"就盲目执行。
- **困惑时立即停止**：指出不清楚的地方并请求澄清，而不是硬着头皮推进后再道歉。
- **对任何非简单任务（3 步以上或涉及架构决策）**，进入计划模式，先写详细规格再执行。

---

## 工作流编排

### 1. 计划优先

- 非简单任务进入计划模式，先写规格，减少歧义。
- 执行过程中遇到问题立即停止并重新规划——不要硬着头皮继续。
- 计划节点同样要包含验证步骤，而非只覆盖构建阶段。

### 2. 精准修改

> 核心原则：每一行修改都应该能直接追溯到用户的请求。

**只碰必须碰的代码，只清理自己造成的混乱。**

- 不要"顺手改进"相邻代码、注释或格式，即使你认为更好。
- 不要重构没出问题的东西。
- 匹配现有代码风格，即使你更偏好不同写法。
- 如发现无关的死代码，**提一下——但不要删除它**，除非被明确要求。
- 因你的改动产生的孤儿代码（失效的 import / 变量 / 函数）要随手清理。

### 3. 自我改进循环

- 每次用户纠正后，把该模式更新到 `Lessons.md`。
- 持续迭代这些经验，直到错误率下降。
- 每次会话开始时，回顾与当前项目相关的经验。

### 4. 完成前验证

- 在证明可用之前，绝不标记任务完成。
- 问自己："资深工程师会认可这个结果吗？"
- 运行测试、检查日志、展示正确性。
- 必要时对比主分支与修改后的行为差异。

### 5. 追求优雅（平衡）

- 对非简单改动：暂停并问"有没有更优雅的实现方式？"
- 如果修复方案看起来很 hack，问"基于现在掌握的一切，如何实现一个优雅方案？"
- 对简单、明显的修复可跳过此步骤——不要过度设计。

### 6. 自主修复 Bug

- 收到 Bug 报告后直接修，不要等用户手把手指导。
- 指出日志、错误信息、失败测试——然后解决它们。
- 遇到 CI 测试失败，直接去修复，无需等待指示。

---

## 目标驱动执行

**将指令式任务转化为可验证的声明式目标。**

| 不要这样说…    | 转化为…                                  |
| -------------- | ---------------------------------------- |
| "添加验证"     | "为无效输入编写测试，然后让它们通过"     |
| "修复这个 bug" | "编写重现 bug 的测试，然后让它通过"      |
| "重构 X"       | "确保重构前后测试都能通过，且行数不增加" |
| "让它工作"     | "定义具体的成功输出，循环验证直到达成"   |

对于多步骤任务，先声明一个简短计划：

```
1. [步骤] → 验证：[检查方式]
2. [步骤] → 验证：[检查方式]
3. [步骤] → 验证：[检查方式]
```

强有力的成功标准让执行循环自主运转；模糊标准（"让它工作"）只会带来反复澄清。

---

## 核心原则

- **简洁优先**：用最少的代码解决问题。
  > 检验标准：资深工程师会觉得这过于复杂吗？如果是，简化。如果 200 行能写成 50 行，重写它。
- **拒绝偷懒**：找到根因，不做临时修补，按资深开发标准执行。
- **最小影响**：改动只触及必要部分，避免引入新 Bug。
- **不要过度推测**：不添加未要求的功能、抽象、"灵活性"或"可配置性"。
